import { expect } from 'chai'
import Svg2Js from '../src/index.js'

describe('Svg2Js', () => {
  let toolit

  before(() => {
    toolit = IconToolkit
  })

  after(() => {
    toolit = undefined
  })

  // it("should return name", function() {
  //   expect(person.getName()).to.equal('Cam');
  // });
  //
  // it("should return age", function() {
  //   expect(person.getAge()).to.equal(99);
  // });
})
